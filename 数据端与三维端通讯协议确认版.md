# 数据端与三维端通讯协议确认版

本文档基于《数据端与三维端通讯协议文档》以及与数据端的确认结果整理，作为当前实现与联调的统一口径。

## 1. 基础结构

所有 JSON 消息统一使用以下 envelope：

```json
{
  "uuid": "uuid-v4-string",
  "timestamp": 1698765432100,
  "type": "String",
  "data": {}
}
```

### 字段约定

- `uuid`：消息唯一标识，用于追踪。
- `timestamp`：消息发送时间，使用 `UTC` 毫秒时间戳。
- `type`：消息类型标识。
- `data`：具体业务数据。

## 2. 下行消息

### 2.1 `TASK_TIME_INFO`

用于初始化或更新当前三维场景的播放时间范围。

```json
{
  "uuid": "a1b2c3d4-...",
  "timestamp": 1698765432100,
  "type": "TASK_TIME_INFO",
  "data": {
    "taskId": "flight_20231031_001",
    "startTime": "2023-10-31 08:00:00.000",
    "endTime": "2023-10-31 09:00:00.000"
  }
}
```

#### 约定

- `taskId` 可以为空。
- 当 `taskId` 为空时，数据端默认发送第一个任务的数据。
- `startTime` 和 `endTime` 为本地时间，保留到毫秒。

### 2.2 `TRACK_POSITIONS`

用于推送航迹位置数据。

#### 结构约定

- 层级固定为 `tracks -> plans -> positions`
- `positions` 下的字段允许增加或删减

#### 说明

- `tracks`：航迹数组
- `plans`：飞行计划数组
- `positions`：位置点数组

### 2.3 `CYCLE_TABLE_DATA`

用于三维端渲染周期表数据。

#### 约定

- `records` 是单条对象，不是数组。
- `records` 中实际字段以表格实际列名为准。
- 周期表和状态表都建议通过映射表处理字段，不要写死业务字段名。

### 2.4 `TIMETABLE_STATE`

用于三维端渲染时间表状态。

#### 约定

- `flightStates` 的结构会动态变化，不固定字段。
- `flightStates` 和周期表都应按实际表字段来解析。
- 实际经纬高等字段，以表中真实列名为准，这里仅作为示例字段。

## 3. 上行消息

### 3.1 `SET_PLAYBACK_SPEED`

设置播放倍速。

### 3.2 `START_DATA_STREAM`

开始发送数据。

#### 约定

- `taskId` 可以为空。
- 当 `taskId` 为空时，数据端默认发送第一个任务的数据。

### 3.3 `PAUSE_DATA_STREAM`

暂停发送数据。

### 3.4 `STOP_DATA_STREAM`

停止发送数据。

### 3.5 `SEEK_TO_TIME`

跳转到某一时刻。

#### 约定

- `targetTime` 为本地时间，保留到毫秒。

## 4. 错误处理

- 错误消息格式已在原文档中补充。
- 实现时建议统一采用与普通消息一致的 envelope 结构，便于记录、追踪和调试。

## 5. 实现建议

1. `timestamp` 统一按 `UTC` 毫秒处理。
2. `startTime / endTime / flightTime / targetTime` 统一按本地时间处理，并保留毫秒。
3. 周期表和状态表都使用“映射表”处理字段，避免写死列名。
4. `TASK_TIME_INFO.taskId` 与 `START_DATA_STREAM.taskId` 均允许为空，空值时由数据端默认选择第一个任务。
5. `TRACK_POSITIONS` 的层级固定，但 `positions` 字段允许扩展。

